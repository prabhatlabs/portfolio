const loading = () => {
    return (
        <div className="fixed top-0 left-0 w-full h-dvh p-4 flex items-center justify-center font-mono animate-pulse">
            prabhatlabs.dev
        </div>
    );
};

export default loading;

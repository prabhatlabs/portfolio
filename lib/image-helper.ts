export function getFullImageUrl(coverImage?: string) {
    // const cloudinaryBaseUrl = process.env.NEXT_PUBLIC_CLOUDINARY_BASE_URL;
    // return coverImage && !coverImage.startsWith("http") && cloudinaryBaseUrl
    //     ? `${cloudinaryBaseUrl}${coverImage}`
    //     : coverImage;
    return coverImage
}
